part of dripicons_flutter;

class DripIconsWeatherData extends IconData {
  const DripIconsWeatherData(int codePoint)
      : super(
          codePoint,
          fontFamily: "DripIconsWeather",
          fontPackage: "dripicons_flutter",
        );
}
